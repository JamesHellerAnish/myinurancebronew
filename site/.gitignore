# Astro build output and caches
dist/
.astro/
node_modules/

# Generated by scripts/sync-public.mjs from the repo root — never edit here, and never
# commit. The originals at the repo root are the single source of truth (§4.3).
public/
